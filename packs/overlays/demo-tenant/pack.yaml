pack_id: demo-tenant
version: "0.1.0"
kind: tenant
description: >-
  Demo tenant overlay: org-specific aliases on existing concepts, one
  detector threshold tuned within its governed range, and one presentation
  preference — exactly the override classes design section 5.4 permits.
