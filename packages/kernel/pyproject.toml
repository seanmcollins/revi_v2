[project]
name = "revi-kernel"
version = "0.1.0"
description = "Shared kernel: scope objects, filters, probes, frames, refs, watermarks, grades, cohorts, money, errors."
requires-python = ">=3.12"
dependencies = [

]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/revi_kernel"]
