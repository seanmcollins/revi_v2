[project]
name = "revi-pack-contracts"
version = "0.1.0"
description = "Public DTOs for the pack-registry capability."
requires-python = ">=3.12"
dependencies = [
    "revi-kernel",
    "pydantic>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/revi_pack_contracts"]

[tool.uv.sources]
revi-kernel = { workspace = true }
