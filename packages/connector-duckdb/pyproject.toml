[project]
name = "revi-connector-duckdb"
version = "0.1.0"
description = "DuckDB AnalyticalRepository adapter: probe compilation, as-of snapshot resolution, cohort materialization."
requires-python = ">=3.12"
dependencies = [
    "revi-kernel",
    "revi-catalog-contracts",
    "revi-calculation-contracts",
    # AnomalySource port + AnomalyRecord live in the investigation
    # application ports (the portfolio surface reads through them).
    "revi-investigation",
    "duckdb>=1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/revi_connector_duckdb"]

[tool.uv.sources]
revi-kernel = { workspace = true }
revi-catalog-contracts = { workspace = true }
revi-calculation-contracts = { workspace = true }
revi-investigation = { workspace = true }
